const sidebar=document.getElementById('sidebar');const menuButton=document.getElementById('menuButton');const search=document.getElementById('search');const navLinks=[...document.querySelectorAll('.nav a')];const sections=[...document.querySelectorAll('article section')];menuButton?.addEventListener('click',()=>sidebar.classList.toggle('open'));navLinks.forEach(link=>link.addEventListener('click',()=>sidebar.classList.remove('open')));document.addEventListener('click',event=>{if(window.innerWidth>860)return;if(!sidebar.contains(event.target)&&event.target!==menuButton)sidebar.classList.remove('open')});const observer=new IntersectionObserver(entries=>{for(const entry of entries){if(!entry.isIntersecting)continue;navLinks.forEach(link=>link.classList.toggle('active',link.getAttribute('href')===`#${entry.target.id}`));}}, {rootMargin:'-25% 0px -65% 0px',threshold:0});sections.forEach(section=>observer.observe(section));search?.addEventListener('input',()=>{const q=search.value.trim().toLowerCase();sections.forEach(section=>{const hay=(section.dataset.search||'')+' '+section.innerText;section.style.display=!q||hay.toLowerCase().includes(q)?'':'none';});});